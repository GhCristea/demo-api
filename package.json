{
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.res.js",
    "build": "rescript build",
    "build:watch": "rescript build -w",
    "typecheck": "rescript build"
  },
  "dependencies": {
    "rescript-schema": "^2.14.0"
  },
  "devDependencies": {
    "rescript": "^11.1.4"
  },
  "_note": "Pure ReScript + Bun. No external HTTP or database libraries. Everything is built-in or ReScript."
}
